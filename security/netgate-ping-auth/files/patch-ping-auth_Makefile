--- ping-auth/Makefile.orig	2018-06-05 13:59:43 UTC
+++ ping-auth/Makefile
@@ -1,3 +1,4 @@
+PREFIX?=	/usr/local
 
 OBJS=atecc.o ping.o ping-auth.o
 INCLUDES=atecc.h ping.h
@@ -19,4 +20,6 @@ ping-auth: $(OBJS) $(INCLUDES)
 clean:
 	rm -f ping-auth ping-auth.core $(OBJS)
 
-#install: all
+install: all
+	install -d -m 0755 $(DESTDIR)$(PREFIX)/bin
+	install -s -m 0755 ping-auth $(DESTDIR)$(PREFIX)/bin
