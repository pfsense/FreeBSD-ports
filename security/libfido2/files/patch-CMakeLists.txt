--- CMakeLists.txt.orig	2024-12-19 12:48:07 UTC
+++ CMakeLists.txt
@@ -71,7 +71,7 @@ if(NOT MSVC)
 endif()
 
 if(NOT MSVC)
-	set(FIDO_CFLAGS "${FIDO_CFLAGS} -D_POSIX_C_SOURCE=200809L")
+	set(FIDO_CFLAGS "${FIDO_CFLAGS} -D_POSIX_C_SOURCE=202405")
 	set(FIDO_CFLAGS "${FIDO_CFLAGS} -D_BSD_SOURCE")
 	if(APPLE)
 		set(FIDO_CFLAGS "${FIDO_CFLAGS} -D_DARWIN_C_SOURCE")
