--- src/Makefile.am.orig	2018-02-14 21:01:26 UTC
+++ src/Makefile.am
@@ -73,6 +73,14 @@ pkg_static_LDADD= @OS_LDFLAGS@ \
 			-lcrypto \
 			-lm
 
+if HAVE_STATIC_ENGINE
+pkg_static_LDADD+=	-L/usr/local/lib \
+			-lateccx08 \
+			-lcryptoauth \
+			-lthoth \
+			-lpthread
+endif
+
 if HAVE_ELF_ABI
 if LIBELF_BUNDLED
 pkg_static_LDADD+=	$(top_builddir)/external/libelf_static.la
