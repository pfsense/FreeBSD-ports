mDNS Bridge is a daemon for sharing Multicast DNS (mDNS) information across
across multiple network interfaces. This allows local DNS Service Discovery
(DNS-SD) across multiple network segments. Commonly used for sharing network 
print services, audio/video streaming, home automation, etc.
